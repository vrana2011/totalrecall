extends layout

block content

    h1 Your past conversations

    form(action="/recordings")
        input(type="search", placeholder="search your conversations", name="searchTranscripts", size="100") 
        input(type="submit")
    
    ol
    each recording in recordingsList
        li= recording.title
 