extends layout

block content

    h1 TotalRecall

    form(action="/recordings")
        input(type="search", placeholder="search your conversations", name="q", size="100", style="font-size: 24px;") 
        input(type="submit" style="font-size: 24px;")a(href="/") Capture a conversation for later recall
   
    
    if recordingsList.length > 0
        h2 Search <em>results</em> from past conversations
        ol
        each recording in recordingsList
            li= recording[0].title
                p!= recording[0].transcript
 