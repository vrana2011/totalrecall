extends layout

block content

    h1 Your past conversations

    form(action="/recordings")
        input(type="search", value="search your conversations", name="searchTranscripts") 
        input(type="submit")
    
    ol
    each recording in recordingsList
        li= recording.title
 